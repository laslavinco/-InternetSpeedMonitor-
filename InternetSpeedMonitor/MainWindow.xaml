<Window x:Class="InternetSpeedMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InternetSpeedMonitor"
        mc:Ignorable="d"
        Title="Internet Speed Monitor" Height="60" Width="120"
        WindowStartupLocation="Manual"
        Topmost="True"
        WindowStyle="None">

    <Grid Margin="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.45*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="downloadText" Text="v" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="0"/>
        <TextBlock x:Name="downloadTextBlock" Text="{Binding DownloadSpeed}" Grid.Row="1" Grid.Column="0" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock x:Name="uploadText" Text="^" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1"/>
        <TextBlock x:Name="uploadTextBlock" Text="{Binding UploadSpeed}" Grid.Column="1" Grid.Row="1" FontSize="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</Window>
